<%= form_for(drying_method) do |f| %>
  <% if drying_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drying_method.errors.count, "error") %> prohibited this drying_method from being saved:</h2>

      <ul>
      <% drying_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
