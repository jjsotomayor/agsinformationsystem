<%= form_for(humidity_sample, html: {class: 'inside-new-form green-inside-new-form', autocomplete: :off})  do |f| %>
  <% if humidity_sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(humidity_sample.errors.count, "error") %> prohibited this humidity_sample from being saved:</h2>
      <ul>
      <% humidity_sample.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :responsable %>
    <%= f.text_field :responsable %>
  </div>

  <div class="field">
    <%= f.label :Tag %>
    <%= text_field_tag :tag%>
  </div>

  <div class="field">
    <%= f.label :humidity %>
    <%= f.text_field :humidity %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar", class: "btn btn-primary btn-lg btn-block" %>
  </div>
<% end %>
