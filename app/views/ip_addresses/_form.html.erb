<br>
<%= form_for(ip_address, html: {class: 'form-basic', autocomplete: :off}) do |f| %>
<br>
  <% if ip_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ip_address.errors.count, "error") %> prohibited this ip_address from being saved:</h2>

      <ul>
      <% ip_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip %>
    <%= f.text_field :ip %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar",  class: "btn btn-primary btn-lg btn-block" %>
  </div>

  <br>
  <%= link_to 'Volver', ip_addresses_path %>
<% end %>
