<%= form_for(sorbate_sample, html: {class: 'inside-new-form', autocomplete: :off}) do |f| %>
  <% if sorbate_sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sorbate_sample.errors.count, "error") %> prohibited this sorbate_sample from being saved:</h2>

      <ul>
      <% sorbate_sample.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :responsable %>
    <%= f.text_field :responsable%>
  </div>

  <div class="field">
    <%= f.label :Tag %>
    <%= text_field_tag :tag%>
  </div>

  <div class="field">
    <%= f.label :sorbate %>
    <%= f.text_field :sorbate %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar", class: "btn btn-primary btn-lg btn-block" %>
  </div>
<% end %>
