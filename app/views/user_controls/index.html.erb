<div class="container">
<h1>Controles de calidad</h1>

<table class = "table table-striped">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Contraseña</th>
      <th>#log ins</th>
      <th>#muestras</th>

      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_controls.each do |user_control| %>
      <tr>
        <td><%= user_control.name %></td>
        <td><%= user_control.password %></td>
        <td><%= user_control.sign_in_count %></td>
        <td><%= "fix" %></td>
        <td><%= link_to 'Editar', edit_user_control_path(user_control) %></td>
        <td><%= link_to 'Eliminar', user_control, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo control de calidad', new_user_control_path %>
