<h1>Nueva sesion de usuario</h1>

  <%= form_tag("/user_controls/create_session") do |f| %>
    <div class="field">
      <%= :name %>
      <%= text_field_tag :name %>
    </div>

    <div class="field">
      <%= :password %>
      <%= text_field_tag :password%>
    </div>

    <div class="actions">
      <%= submit_tag :submit %>
        <!-- <input type="submit" value="Submit">%> -->
    </div>
  <% end %>

<%= link_to 'Back', user_controls_path %>
